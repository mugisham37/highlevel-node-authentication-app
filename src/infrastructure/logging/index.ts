// Main logging module exports
export { createLogger, createLoggerWithCorrelation, logger, createChildLogger } from './logger';
export type { Logger } from './winston-logger';

// Default export
export { default } from './logger';
